import Mathlib

example (a b : ℕ) : a + b = b + a := by
  rw [add_comm]

example (a b : ℕ) : a + b = b + a := by
  rw [add_comm]

example (a b : ℕ) : a + b = b + a := by
  rw [add_comm]

example (a b : ℕ) : a + b = b + a := by
  rw [add_comm]
